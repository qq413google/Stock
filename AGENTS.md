# AGENTS.md — Stock Portfolio Advisor

> AI Agent 进入此目录时**必须先读本文件**，再执行任何任务。

## ⛔ 给接手 AI 的硬性警告（最高优先级，先读这段）

> 这套工具曾因 AI 误判害用户实亏（完整复盘见 `策略共识与验证计划.md` 第七节 + `trades.json`；`trading-watchlist.md` 编码曾损坏、2026-08-14 已还原但丢失约6%字符，仅作存档）。根因：某 AI 用了带 bug 的资金流数据（把"散户接盘"读成"机构抢筹"）、无视风控、喊"黄金坑、赶紧买"。**为防重演，任何 AI 给买卖建议前，必须无条件遵守以下 5 条，违反即视为不可信：**

1. **先读规则再开口**：必须先读 `策略共识与验证计划.md` 与 `references/risk-management.md`，按最新规则分析(当前 v2.14,见规则文件版本历史)；**禁止**凭训练直觉直接给建议。
2. **买入建议必须过闸门 + 出示风险账**：禁买清单全绿 + 落在合法买点 + **必须同时给出止损价、仓位(止损反推)、资金面判定**；给不出，就说"观望/不买"，不准含糊推荐。⚠️v2.13起**盈亏比≥2门槛已废止**(研究Q/R/Q-2证伪:与收益零相关r≈0、方向反了、任何门槛都毁钱)，不得再用它否决买点。
3. **用资金流前必跑恒等式校验**：`node scripts/flow.js <secid>` 输出须满足 `主力 = 超大单 + 大单`；不通过 = 数据有 bug，**立即停用**，不得据此判断。
4. **数据现拉，不信旧数据**：用 `scripts/` 实时拉取；不得用用户粘贴的、可能过时/带 bug 的数据下结论。
5. **绝不制造紧迫感**：禁止"黄金坑/赶紧买/再不买就没了/现在就买"等话术。扣扳机是用户的事；好建议永远附止损与仓位，永远允许"等"。(注:盈亏比门槛v2.13已废止)
6. **警惕"系统PASS"引发的锚定效应，空间条件不等于时空条件**（2026-07-20 药明康德教训，见`策略共识与验证计划.md`九）：看到 `confirm.js` PASS / 均线多头排列后，人容易从"找理由质疑"切换成"找理由确认"——方向一旦反了，后面每条检查都会变成"看能不能打勾"。尤其要分清**空间条件**（现价与MA10的距离够近）和**时空条件**（价格在该位置企稳过一段时间）：下跌途中路过均线 = 满足空间条件，但不满足时空条件，不算回踩买点。**给"可以买"结论前必须依次核实三层，缺一不可**：①近3日K线方向——排除"均线多头排列是被更早的大涨拉高、长期均线滞后未转向"掩盖了实际破位下跌；②现拉 `trends.js` 分时——排除"贴近均线是下跌路过，不是止跌企稳"（连续创新低 ≠ 企稳）；③以上两层都过，才叠加止损/盈亏比/资金面走完整闸门。三层任一层没查就下结论 = 又是一次锚定。**(2026-08-07更新: confirm.js v2.9已把三层焊进代码——近3日破位检查/分时企稳判定/9:30-10:00噪声窗PASS降级WAIT/盘中量能按时段折算,分时拉不到一律不给PASS。但这是防御纵深不是豁免——AI给"可以买"结论前的人工三层核实义务不变,PASS依然只是"来看"。)**

   > 🔁 **同一个漏洞已在三处独立出现——发现新代码时主动去找第四处（2026-08-14）**：
   > "空间条件≠时空条件"不是某个函数的局部bug，而是一类**反复重现的认知盲区**——
   > 凡是"用某一瞬间的价格位置下判断"的地方，都可能踩到。已发现并修复的三处：
   >
   > | # | 位置 | 症状 | 修复 |
   > |---|---|---|---|
   > | 1 | `confirm.js` 的 atBuyPoint | 把"下跌路过MA10"判成回踩买点 | v2.9 加分时企稳+近3日方向 |
   > | 2 | `arm-positions.js` 移动止损 | 浮亏时也用MA10收紧止损(回测-21.6万/回撤115%) | v2.12 剔除MA10 |
   > | 3 | `check-alerts.ps1` 的 `op:'>'` 触发器 | 把"从上方跌来还没破线"判成"从下方反弹收复"(德赛西威实例) | 2026-08-14 加 reclaim guard(要求昨收≤线或当日最低≤线) |
   >
   > **共同形态**：只用 `当前价 vs 某价位` 这一个瞬间读数，就断定价格"处在某种状态"，
   > 而不问"它是怎么到这儿的"。**排查方法**：看到任何 `price > X` / `price < X` / `|price-X|<ε`
   > 形式的判定，先问"从上面来的和从下面来的，这里区分了吗？"——若没区分，大概率是第四处。

> 一句话：**给不出止损/仓位/资金面、且没跑恒等式校验的买卖建议 = 噪音，不准发给用户。**(注:盈亏比v2.13已废止,不再是必答项)

7. **新增/修改任何规则时，必须当场标注它的证据等级**（2026-08-10 v2.8保本位条款被证伪的教训）：`[回测验证]`（写明研究编号+样本量）/ `[实盘教训]`（写明案例）/ `[逻辑推演·未验证]`。**自创的"听起来合理"的约束，必须标 `未验证` 并进 TODO 待测**——v2.8 我加的"加仓后整体止损≥保本位"就是典型：逻辑无懈可击（不让加仓吃掉原仓利润），回测却证明它是所有方案里最差的（总盈亏 -28,643、回撤 15.2%→25.1%，两个行情半段一致），因为它把止损抬到技术支撑之上，导致本可继续跑的仓位被正常回踩扫出。**更严重的是：我还用这条未验证的自创规则去阻止用户做一个规则本身放行的操作**——拿自己的假设当既定事实压用户，是这套系统最该防的行为。

## 这里是什么

这是 A股持仓管理与选股分析的工作目录。包含一个完整的 stock-portfolio-advisor skill 及其所有依赖文件。

## 目录结构

```
D:\AI\workspace\stock\
├── AGENTS.md              ← 你正在读的文件
├── SKILL.md               ← Skill主文件（触发条件、工作流、输出规范）
├── references/
│   ├── risk-management.md ← 风控规则（建仓/止损/仓位/利好叠加判定）
│   ├── technical-analysis.md ← 技术分析参考
│   └── eastmoney-api.md   ← 东方财富API文档
├── scripts/               ← Node.js数据获取脚本
│   ├── quote.js           ← 实时报价
│   ├── flow.js            ← 主力资金流向
│   ├── kline.js           ← K线数据
│   ├── trends.js          ← 分时走势
│   ├── sector.js          ← 板块涨幅(成交额用f6)
│   ├── index.js           ← 三大指数
│   ├── calc.js            ← 技术指标 MA/RSI/MACD/KDJ
│   ├── market.js          ← 大盘环境分级→仓位上限
│   ├── positions.js       ← 账户级风控校验
│   ├── stats.js           ← 交易复盘统计
│   ├── events.js          ← 除权除息/解禁/财报
│   ├── arm-alerts.js      ← 每日自动布防(从watchlist挑票→alerts.json)
│   ├── arm-positions.js   ← 持仓自动布防(读positions.json→止损/移动止损/加仓观察)
│   ├── overseas.js        ← 隔夜外盘beta(纳指/标普/道指+半导体篮子代理费半)
│   └── confirm.js         ← 回踩三项确认(主力/缩量/MA10+分时企稳+近3日方向, 供盯盘调用)
├── backtest/              ← 回测引擎(lib.js)与各项研究(studies*.js + results*.json)
├── cache/                 ← backtest/(回测K线资金流缓存) intraday/(confirm当日快照)
├── watchlist.json         ← 自选股池(39只, 手动维护, 不自动变)
├── alerts.json            ← 盯盘触发器(arm-alerts每日自动刷新)
├── positions.json         ← 当前持仓与账户状态
├── trades.json            ← 已平仓交易记录
├── events.json            ← 事件日历(手动维护)
├── tasks/                 ← 零token定时盯盘(check-alerts.ps1 每1分钟/run-hidden.vbs/alerts.log/alerts-state.json)
└── assets/                ← 静态资源
```

> 📦 **要在新机器上从零部署（建定时任务/盯盘/初始化数据文件）？→ 看 [SETUP.md](SETUP.md)**，本文只讲怎么用。

## 🚦 新接手的 AI：先看这张表（用户说什么 → 你做什么）

> 你的角色：**A股实盘辅助决策助手**。用户是散户（5万本金，当前小仓位实盘验证阶段）。
> 你的产出永远是「**现拉数据 → 过闸门 → 给带止损与仓位的结论**」，不是聊天式意见。
> **当前持仓/现金看 `positions.json`；已平仓战绩跑 `node scripts/stats.js`。**

| 用户说 | 你做 |
|---|---|
| 「今天怎么操作」「现况如何」 | ①`overseas.js`(隔夜外盘beta) ②`market.js`(大盘分级→仓位上限) ③`positions.js`(持仓+触线检查) → 给「持仓怎么办 + 今天能不能出手」；**盘中报价必带时刻**(早上的分级中午就可能失效) |
| 「XX股票怎么样」「能买吗」 | **反着查，找坑不找勾**：①`kline.js`看近3日方向(排除破位下跌被滞后均线掩盖) ②`trends.js`逐行看分时(排除下跌路过≠企稳) ③`flow.js`(恒等式+量级校验) ④`calc.js` → 过全闸门 → **给不出止损/仓位/资金面就说观望** |
| 「弹窗了」(贴图或转述) | 看弹窗标注：`PASS+AUTO-CONFIRMED`=系统三项过了但**仍须人工三层核实**；`PRICE-ONLY`=只是价到了；`[!]噪声窗`=9:30-10:00只看不动；`[sell]`=止损类**最高优先级**。**一律现拉数据复核，不信弹窗里的旧价** |
| 「我买了/卖了X」 | ①问准成交价 ②改 `positions.json`(持仓+现金+账本) ③平仓的记 `trades.json`(含守纪律与否+教训) ④跑 `node scripts/arm-positions.js` 重布防 ⑤`node scripts/positions.js` 校验 ⑥**必做：`node scripts/audit.js` 事后自查**(见下节) |
| 「战绩/复盘」 | `node scripts/stats.js`；**注意看纪律执行率，那才是当前阶段的KPI**，不是盈亏 |
| 「优化/改规则」 | **先分清这条是[回测验证]还是[逻辑推演]**（硬性警告第7条）；放宽类改动次一交易日生效；想加新信号先查 `策略共识与验证计划.md` 第三节「不要做的事」 |
| 「为什么涨/跌」 | 可用 WebSearch，但**只复述有出处的，不编叙事**；外盘/新闻**只做环境校准与排雷，绝不做买入理由** |

**每日节奏**：09:25 盘前看外盘 → 09:30-10:00 噪声窗只观察 → 10:00 第一决策点 → 14:50 收盘前必查持仓 → 盘后记账。

---

## 🔍 每笔操作后强制自查（不许等用户来问）

> **立此条的原因（2026-08-14）**：K/M/N/O/P 五次回测**全部由用户推动**，Claude 一次都没主动发起。
> 而策略共识 2026-07-01 就写过同样的教训（"都是用户逼出来的，不是自发发现的"）——
> **一个半月后仍在重犯，说明写进文档没用，必须变成流程里的强制动作。**
> 京东方那个止损 bug 若不是用户追问"才亏1个多点合理吗"，会一直在实盘跑（统计后果 −21.6万/回撤115%）。

**每次买卖成交、每次触发止损/止盈之后，立刻跑 `node scripts/audit.js` 并回答这四问：**

1. **参数漂移**：实际执行值 vs 建仓时写下的计划值，有无偏差？
   （京东方案例：计划止损 5.76，实际被 5.85 扫出 —— 这类偏差就是 bug 的信号）
2. **依据强度**：本次决策用到的每条规则，是**回测验证过**还是**逻辑推演**？
   推演的必须当场标注，并主动提议"要不要测"——**不许当既定事实用**。
3. **实盘 vs 回测口径**：实盘用的公式与 `backtest/` 里验证时的口径是否逐字一致？
   （不一致 = 在跑一个从未被验证的策略，京东方的 MA10 就是这么混进去的）
4. **反直觉检查**：如果结果符合预期，问"是 process 对还是运气好"；
   如果不符合预期，问"是策略错了还是执行错了还是规则本身错了"。

**发现任一项异常 → 当场提出回测方案，不等用户开口。**

---

## 启动检查清单

进入此目录后，按顺序执行：

0. **先读 `策略共识与验证计划.md`** — ⭐ 最新共识：当前处于「小仓位实盘验证」阶段，核心认知 + 5条验证规程 + 不要做的事。任何买卖/优化决策先对照它。
1. **读 `SKILL.md`** — 了解工作流、分析模板、决策树
2. **读 `references/risk-management.md`** — 风控规则是红线，不可违反
3. **读 `references/technical-analysis.md`** — 技术分析框架
4. **读 `references/eastmoney-api.md`** — API接口文档（需要拉数据时查）

## 核心风控规则（速查 · v2.15）

> 完整规则见 `references/risk-management.md`（**唯一规则源，当前 v2.15**；本速查可能滞后，冲突时以规则文件为准），以下为高频条款：

**仓位**：中低价股单笔风险=账户1.5%(~750元)，可买金额=750/止损距离%；**只买股价≤180元的票**(单手≤1.8万)，华工175在内、新易盛540等剔除；高价股只买1手不加码、-8%硬顶控风险(180元1手≈36%/最坏亏2.9%)；单赛道≤50%、持仓≤3只
**买点**：必须落在三类买点之一（回踩支撑/突破回踩/超跌反包），否则不买
**禁买**：硬性——**当日涨>3%**(v2.14,原5%,研究S:>3%档回撤13.6%/每笔135元 vs >5%档23.4%/126元,按用户"扛不到32%回撤"的风险偏好收紧;⚠️此条实际只拦超跌反包,顺势回踩涨幅天然<3%从未被拦)；**主力净流出**=逆势(空头/破MA60)硬禁，多头排列回调中的流出不一票否决(看温和缩量/还是放量派发，详见 risk-management v2.2)。软提醒(2026-07-10回测降级,不再一票否决)——破位大阴线、顶部首跌抄底、板块退潮逆势接(详见 risk-management v2.3)
**盈亏比**：🔴**v2.13已废止**——研究Q/R/Q-2三次证伪(与收益相关系数r≈0；RR0.3~0.5档每笔+631元 vs RR≥1.5档+126元，方向反了；任何门槛含0.3都在毁钱)。机制:盈亏比是"强弱"的反向指标(低RR=接近前高=强势股)。**不得再用它否决买点**；止损仍必须写下(用于反推仓位+出场)
**加仓**：仅浮盈≥5%向上加，金字塔减量，**严禁向下摊薄**；豁免「当日涨>5%」禁买(v2.8)，单票加1次；**止损口径=维持原止损 + 移动止损**(v2.9废止"≥保本位",回测K证伪:保本位-28643元/回撤25.1%,维持原止损+15556元/回撤14.6%)；⚠️**移动止损公式见下方"止盈"条,v2.12已把MA10剔除**(v2.9原文的"跟MA10"已作废)；**门槛5%已回测验证**(研究M:倒U型峰值,0%/+12769 5%/+14118 10%/+5300,勿调)；~~**加仓自身盈亏比≥2**(v2.10)~~ **v2.13降级为参考**(建立在已被证伪的盈亏比概念上,加仓场景未单独回测)；**形态优先回踩>突破**(回踩胜率85%/零额外回撤 vs 突破69%/回撤+1.7点),走突破形态**限价必须挂突破位上方等回踩,禁在冲高处接,禁以"确保成交"为由把限价设在现价上方**(v2.9b)。**元规则：放宽进场门槛次一交易日生效；收紧类、及被回测证伪的规则纠错，即时生效**
**止损**：技术位为主，放量破位即清；单股硬顶 -8%；MA20 仅减仓；14:50前必查不留隔夜；**距离维持 `min(MA20,价×0.97)` 不放宽**(v2.15研究T:12种口径对比,现行总收益第二且两半段都排前三=均衡最优;固定4%收益虽最高但绞肉市更差、回撤高4.1点)。🔴**止损误杀率60~70%是常态**——现行口径65%的止损会在5日内涨回买入价上方,放宽到6%仍有59%。**再遇"止损后连涨"先回看这张表,别改止损距离**(京东方8/14即落在这65%里)
**尾部与结构**(v2.7)：①~~盈亏比目标位必须可指认~~(v2.13随盈亏比门槛一并废止)②-8%非真地板,T+1跳空可击穿,真实最坏亏>模型,临事件降仓/空仓过夜③高相关赛道(光通信/PCB/半导体/消费电子同吃AI硬件beta)合并算50%敞口④停牌=止损失效,复牌按新技术位重判不死扛(详见 risk-management 第十三节)
**开盘噪声窗**(v2.6)：9:30–10:00只观察不动手（弹窗照弹但别凭开盘瞬间价扣扳机）——卖需「跌破当日最低」或「10:00后仍在止损下方」，买需价位「扛过10:00+confirm确认」；仅 -8%灾难线/基本面利空跳空 可开盘即走。是药明康德「空间≠时空」的对称面（下跌路过≠破位/拉升路过≠站稳）。隔夜外盘(纳指/费半)只做**beta环境**，不做买入理由（详见 risk-management 第六·补节）
**止盈**：**移动止损口径(v2.12,研究P)——持仓期收盘浮盈曾≥5%后:止损=max(原止损, 保本位, 持仓期峰值收盘×0.93)；未达5%维持原止损不动。🔴MA10严禁进入移动止损**(含MA10:+27,188/回撤40%;无条件MA10:−59,328/回撤115%会爆仓;不含MA10:+157,339/回撤23.4%)。⚠️"震荡票到目标位分批"**存疑待确认**(研究R:目标位出场−15,535 vs 移动止损+155,757,连震荡票也是移动止损更好+147,671 vs −9,805)
**账户熔断**：日亏3%停手 / 周亏6%暂停 / 连亏3笔空仓2日
**保留**：买入不限时段、利好叠加(≥3)动态调整、分时缩量分层、连续空仓自检
**数据**：用前校验恒等式 `主力=超大单+大单`，量级常识检查

## 用户画像

- 总资金：5万元
- 交易风格：严格执行止损，空仓观望等待机会
- ⚠️ **当前状态不写死在本文件** —— 持仓/现金/仓位一律**现读 `positions.json`**，战绩跑 `node scripts/stats.js`。
  （本行曾长期写着"空仓，可用48,632元"而实际已持仓，2026-08-14 全文档审查时发现。**静态快照必然过时，不要在文档里复制账户状态**。）
- **当前阶段：小仓位实盘验证中**（目标攒够30笔交易，用 stats.js 验证真实期望值；详见 `策略共识与验证计划.md`）
- 关注范围：自选池 `watchlist.json` 全体，**一视同仁，无单一赛道偏好**（不特别偏光通信或任何赛道）；优先看池内，不追池外题材

## 数据脚本用法

```bash
# 实时报价（secid: 沪市1.XXXXXX, 深市0.XXXXXX）
node scripts/quote.js 1.600487

# 主力资金流向
node scripts/flow.js 1.600487

# 近10日K线
node scripts/kline.js 1.600487

# 分时走势
node scripts/trends.js 1.600487

# 板块涨幅TOP 10
node scripts/sector.js 10

# 三大指数
node scripts/index.js

# 技术指标 MA/RSI/MACD(DIF/DEA)/KDJ
node scripts/calc.js 1.600487

# 大盘环境分级(→建议总仓位上限)
node scripts/market.js

# 隔夜外盘beta环境(纳指/标普/道指 + 半导体篮子代理费半; 仅环境不做买入理由)
node scripts/overseas.js

# 持仓自动布防(读positions.json→挂硬止损/移动止损(保本位与峰值x0.93,无MA10)/冲高回落止盈/加仓观察; --dry预览)
node scripts/arm-positions.js

# 多日资金流(连续N日流向)
node scripts/flow.js 1.600487 5

# 账户级风控校验(读 positions.json)
node scripts/positions.js

# 交易复盘统计(读 trades.json)
node scripts/stats.js

# 事件日历(读 events.json)
node scripts/events.js

# 每日自动布防(从watchlist挑"站MA60+站MA20+支撑≤180"的票→alerts.json; --dry只预览)
node scripts/arm-alerts.js

# 回踩三项确认(主力转正/缩量/不破MA10; 盯盘 confirm:true 触发时自动调用)
node scripts/confirm.js 0.002463
```

> 数据可用性(本环境)：实时北向、全市场涨跌家数 **不可得**（接口受限/已停发），相关脚本逻辑就绪，到真实环境即生效。多日资金流 **已可用**（push2his fflow/daykline接口）。

## 盯盘与每日自动布防（tasks/ + arm-alerts.js + confirm.js）

零 token 的价格盯盘：**Windows 定时任务 `StockAlerts` 每 1 分钟**（2026-08-06 由3分钟改为1分钟，`Interval=PT1M`）跑 `tasks/check-alerts.ps1`，碰触发线弹窗。**不耗 token，LLM 只在用户拿弹窗来问时才介入。**

> 查看/修改定时任务（改 Interval 需**管理员** PowerShell）：
> ```powershell
> Get-ScheduledTaskInfo -TaskName "StockAlerts"                          # 看上次运行/结果/下次
> (Get-ScheduledTask -TaskName "StockAlerts").Triggers[0].Repetition     # 看频率
> powershell -ExecutionPolicy Bypass -File tasks\check-alerts.ps1 -Test  # dry-run(不弹窗/不写状态)
> ```

**两层结构：**
- **自选股池 `watchlist.json`（39 只）**：手动维护，**不自动变**，也不扫全市场。
- **每日布防 `alerts.json`**：每天开盘首次运行由 `arm-alerts.js` 从池子**自动挑选并刷新价位**，天天变。

**每日布防入选依据（arm-alerts.js，三条全过才布防）：**
1. 收盘 > MA60（长期趋势向上/顺势）
2. 收盘 > MA20（趋势未破，非破位）
3. 支撑位（回踩价 MA10 或 MA20）≤ 180 元
→ 破 MA60/MA20（弱势/破位）、支撑 > 180（超价）**自动剔除**；手动钉的单独保留。

**盯的价位（每天重算，不过时）**：支撑位 = MA10（价在其上时）或 MA20。

**alerts.json 触发器字段：**
- `posauto:true` — `arm-positions.js` 生成的**持仓**触发器（止损/移动止损/冲高回落止盈/加仓观察），每次重跑整批替换；arm-alerts 不碰它们，持仓票也不再布买入线
- `sell:true` — **卖出类**：崩盘门(3指数均值≤-1.5%)静音所有买入提醒时**它照弹**；弹窗用急促音效+Warning图标+"STOP/SELL"标题
- `spikeFade:true` — **冲高回落止盈**（第五节3）：不是固定价位线，由 check-alerts 盘中实时算 `日内涨幅=(最高-昨收)/昨收 >7%`（v2.11，原5%被研究N/N-2证伪）且 `回落=(最高-现价)/最高 >2%` 才触发；回到距最高1%内自动重新armed。仅 ≥200股(能真正减半)的持仓才布
- `manual:true` — 手动钉，arm-alerts 不覆盖（保留自定义 msg/价位）
- `track:"MA5|MA10|MA20"` — 价位每天跟该均线 ×1.01 自动刷新
- `confirm:true` — 触发时自动跑 `confirm.js` 确认。**硬判定(均回测验证过)**：①当日主力净流入>0(最强,+2.90% vs 净流出+0.70%)②不破MA10 ③缩量 ④多头 ⑤**非RSI超买(>70)**(超买回踩买 +0.50% vs 正常 +2.63%,验证有效)→ `VERDICT(PASS/WAIT/FAIL)`。**软提醒(不改判)**:5日"未修复暴量派发"(净占比≤-8%且5日净流出)只标 `[!]`——回测拦52%且被拦者仍为正,故不当闸门。⚠️ **MACD死叉不作利空**:回测显示死叉回踩反而更好(+3.01% vs 多头+1.74%),勿再当避险理由。买区内每轮自动复测,**转 PASS 时额外弹一次**

**弹窗自我标注**：带 `AUTO-CONFIRMED + VERDICT` = 已自动判定；带 `PRICE-ONLY, ping Claude` = 仅价到了、需人工确认。

> ⛔ **铁律：触发 ≠ 该买。** 即使 confirm 给 PASS，仍必须过 v2.0 全闸门（禁买清单 + 合法买点 + 现拉资金面恒等式 + 止损可执行；**盈亏比门槛v2.13已废止**）+ 定手数/止损后才可下单。alert 只"叫你来看"，不"替你扣扳机"。

## 输出规范

- 价格保留2位小数
- 涨跌幅用百分比，正数加+号
- 资金流向统一用亿元
- 表格优先，结论清晰
- 用表情符号增强可读性：✅利好 ⚠️注意 🔴风险 🟡中性 📈上涨 📉下跌
- **任何"可以买"的结论必须附三件套：①买入价上限（超过即作废，不追）②有效时间窗（如"30分钟内/仅今日上午"）③手数**（2026-08-06 中兴通讯复盘补正立规）：弹窗/分析 → 用户打开券商App下单，中间有几分钟延迟，价格可能已经上冲。绿灯若不带限价，执行延迟会把合法买点变成追高成交——既伤盈亏比，还会让复盘把"流程缺陷"误记成"用户违纪追高"。用户扣扳机时执行的是"AI说可以买"这个指令，**限价和时效是批准方的责任，不是执行方的**。给不出三件套的"可以买"=不完整建议，不准发。
- ~~**盈亏比不达标时必须反推给出"合理买入价上限"**（2026-07-22 恒瑞医药教训）~~ 🔴 **v2.13 整条废止**——它建立在「盈亏比≥2」之上，而该门槛已被研究 Q/R/Q-2 三次证伪。**不得再用"盈亏比不够"否决买点，也不得再用它反推买入价上限。**
  > ✅ **但"给不出价位就别只说不买"这个精神保留**：否决一笔买入时，仍应尽量给出**可执行的替代条件**——例如"等回踩到 MA20 附近再看""等主力转净流入""等涨幅回落到 3% 以内"。区别在于：依据换成**买点定义/禁买清单/止损可执行性**，不再是盈亏比算术。

## 版本历史

- **2026-08-14（全文档一致性审查）**：用户要求"重新阅读所有文档别遗漏"，扫出并修复 **18 处过时/矛盾**——最危险的三类：①**"移动止损跟MA10"在3个文件5处残留**（与 v2.12「MA10严禁进入移动止损」直接冲突，接手AI照做会跑一个回撤115%的口径）；②**用户画像写着"空仓48,632元"而实际已持仓**（已改为"不写死状态，一律现读 positions.json"——静态快照必然过时）；③**两个文件的版本历史完全缺失 v2.11~v2.14**（今天四个重大改动都没进索引，接手AI会以为最新是v2.10b）。其余：速查表版本号 v2.0→v2.14、自选池20只→39只、盈亏比残留5处、"盈亏比反推买入价上限"整条废止（但保留"否决时要给可执行替代条件"的精神）。
- **2026-08-14（规则批次）**：v2.12 移动止损剔除MA10（研究P，修一个会打爆账户的实盘bug）、v2.13 废止盈亏比≥2门槛（研究Q/R/Q-2三次证伪）、v2.14 涨幅禁买5%→3%（研究S，按用户回撤承受力选定）。同日建立 **audit.js 操作后强制自查机制 + AGENTS四问**——起因是 K/M/N/O/P 五次回测全部由用户推动、Claude零主动，而同样的教训2026-07-01就写过。audit首次运行即抓到总资金基准漏更新168.86元。另修：check-alerts 触发器 reclaim guard（"从上方跌来"被误判成"从下方收复"，德赛西威实例）、arm-positions 移动止损未回写positions.json导致K线失败时静默退回。
- **2026-08-13**：v2.11 冲高回落阈值5%→7%（研究N/N-2，原5%实测亏钱）；研究O否决"减半后恢复仓位"（唯一一次回测阻止改动）；研究M验证加仓门槛5%恰在倒U型峰值。泰格医药首次触发冲高回落减半（卖100股@56.00）。
- **2026-08-10~12**：v2.8 加仓豁免涨>5%禁买（用户提出，机制审视成立）；v2.9 废止自创的"加仓后止损≥保本位"（研究K证伪，是所有方案最差）；v2.9b 加仓形态优先回踩>突破；v2.10/6b 加仓自身盈亏比闸门（后于v2.13降级）。确立**规则治理元规则**：放宽进场门槛次一交易日生效，收紧类与被回测证伪的纠错即时生效。
- 2026-08-07（追加2）：confirm.js v2.9 时空治本（审查#5收尾，药明7/20教训的代码级修复）——①分时企稳判定(近10分钟不创当日新低+站回均价线/离开低点≥0.8%,不企稳一律WAIT,分时缺失也WAIT)②近3日方向检查(已完结3根连跌且≥5%→破位型,顺势回踩不给PASS)③9:30-10:00噪声窗内PASS降级WAIT(v2.6焊进代码,10点后自动复测转PASS自动弹)④盘中量能按已交易时段折算(修"盘中必显缩量"失真,5日均量改用已完结日)。当日实证:蓝思9:36盘中PASS(时空未确认)→9:47砸新低,新版该时刻会判WAIT。
- 2026-07-31（追加）：对抗性审查修复批次——机制3个bug（崩盘门放行止损弹窗、positions.js重试容错、arm-positions.js持仓自动布防止损/移动止损/加仓）+ 规则4条补丁 v2.7（盈亏比目标客观化#8、-8%非真地板/T+1尾部#6、高相关赛道合并算敞口#7、停牌应对#11，详见 risk-management 第十三节）。待办：#5 confirm.js时空治本、#9恒等式量级校验、overseas.js隔夜外盘beta。
- 2026-07-31：风控规则 v2.6 + 盯盘弹窗时间标注——泰格医药实盘暴露开盘噪声问题（止损48.02被跳空击穿、下探47.50后V型拉回49，早盘噪声差点导致误割）。新增「开盘30分钟只观察不动手 + 时空确认」规则（risk-management 第六·补节）；`check-alerts.ps1` 弹窗在 9:30–10:00 自动加「NOISE WINDOW」标注提醒别动手。隔夜外盘 beta 层原则一并钉入（数字做环境、不做买入理由，`overseas.js` 待建）。
- 2026-07-22（追加）：修复 `arm-alerts.js` 结构性缺口——中兴通讯暴跌后MA20/MA10支撑位本身已隐含相对昨收>5%涨幅，一旦"站回"/"反弹收复"触发就必然撞上「当日涨>5%」硬性禁买线，这类触发器从生成起就不可能产生合法买点，却照样被布防。改为生成前先算 `支撑位/MA20 vs 真昨收(kl[i-1].c，非当前实时价kl[i].c) 的涨幅`，超5%的直接跳过并标注原因，等差距收窄再自然纳入。同时发现`kl[i].c`在盘中重跑时是实时价不是昨收，用它当基准会随手动重跑时刻漂移——已改用`kl[i-1]`锚定，不受运行时刻影响。
- 2026-07-22：修复 `check-alerts.ps1` 漏弹 bug——"反弹收复"类(`op:'>'`)触发器当天首次armed状态曾用**当前瞬间价**判断(`price<=thr`)，遇到开盘快速拉升/涨停的票(通富微电当日+10%)，脚本第一次轮询到时价已窜过触发线，被误判成"已经fire过"，导致全天不弹、错过反包信号。改用**昨收价**判断(`prevClose<=thr`)——昨收在线下才算今天新穿越，理应armed；不影响已armed/已发过的状态延续。每日armed状态本就在跨日时清空重置，此修复只影响今后交易日，无法回补当天已发生的错过。
- 2026-07-10：风控规则 v2.3——按组合级回测（3仓位+5万账户+0.15%成本约束）结果，禁买清单中「破位大阴线/顶部首跌/板块退潮」三条降级为软提醒（回测无预测力），止盈规则「资金流出货=减仓/清仓」废止（回测显示卖了即踏空）；同批回测确认买点定义/≤180价格上限/趋势策略骨架不变。详见 `references/risk-management.md` v2.3 与 `backtest/回测研究_组合级最终修正版_20260710.md`。
- 2026-07-01：盯盘系统升级——check-alerts 改**边沿触发**(多次触发都弹/贴线不刷屏/状态先于弹窗写防重复)；新增 `arm-alerts.js`(每日自动布防 + `track` 价位自动刷新)、`confirm.js`(回踩三项确认)；`confirm:true` 触发自动确认 + **转PASS自动复弹**(无需用户喊)；弹窗**自我标注**(AUTO-CONFIRMED / PRICE-ONLY)。规则 v2.2：主力净流出禁买改**趋势前提制**(空头硬禁/多头回调看缩量还是派发) + 「涨>5%」澄清(盈亏比代理，**不放宽到7%**)。
- 2026-06-29：风控规则 v2.0 系统性重构——修复加仓矛盾(改金字塔向上加)、新增买点定义+禁买清单、仓位改"止损反推"模型、止损改技术位为主、止盈加移动止损、新增盈亏比≥2门槛/账户级熔断/复盘闭环、评分改四维+流动性前置；修复 index.js 与 flow.js 两个取数脚本 bug
- 2026-06-25：删除"14:30后不开仓"错误规则，新增利好叠加判定、动态盈亏比、分时缩量条件、连续空仓自检等6条规则
