# 缓存（API 拉取的数据，每次可重新生成）
cache/backtest/
cache/intraday/

# 平台私有不提交
.workbuddy/
node_modules/

# 盯盘运行时状态（自动生成）
tasks/alerts-state.json

# 盯盘日志（自动生成）
tasks/alerts.log
tasks/*.log
tasks/*-watchlist.log

# 独立 dashboard 工程（归属存疑/疑似他处生成，暂不纳入版本控制，留原地）
dashboard_template.html
dashboard_locales.py
render_dashboard.py
export_results.py
index.html

# 其他工具留下的碎文件
_git_push_result.txt
_pwsh_test.txt

# OS
.DS_Store
Thumbs.db
